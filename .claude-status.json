{
  "updated": "2026-04-25T14:21:00Z",
  "last_task": "fix-mozart-add-project-reference",
  "status": "done",
  "working": [
    "add_project_reference: uses supabase client, writes to songs.work_data.reference_links",
    "project_name resolution: ilike title match → fallback to most recent song → error if nothing found",
    "Full error logging at every step with [mozart] prefix",
    "Duplicate check by reference_track_id OR (title+artist)",
    "mozartTools payload description updated: title/artist/spotify_id instead of track string",
    "Smoke test confirmed: project_name=nidjo resolved song and wrote to DB"
  ],
  "broken": [],
  "partial": [
    "BUILD 2/3: feedback_patterns and chart_history tables still need SQL in Supabase",
    "genres column: ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS genres jsonb;"
  ],
  "next": "Run ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS genres jsonb; in Supabase, then POST /enrich-library-genres to backfill",
  "notes": "Root cause of silent failure: previous handler wrote to songs.reference_links (top-level column) via direct fetch. New handler writes to songs.work_data.reference_links via supabase client, which is what ProjectsTab reads."
}
