{
  "updated": "2026-04-25T22:31:00Z",
  "last_task": "fix-spotify-user-token-playlist",
  "status": "done",
  "working": [
    "spotifyFetch(): already used spotifyUserToken || getSpotifyToken() — confirmed",
    "fetchSpotifyId(): now routes through spotifyFetch() instead of raw getSpotifyToken()",
    "importSpotifyPlaylist(): already used spotifyFetch() throughout — no change needed",
    "pulse-check parallel prefetch: now uses spotifyUserToken when available",
    "All Spotify calls use user token first, client token as fallback",
    "pm2 restart clean — watcher online"
  ],
  "broken": [],
  "partial": [
    "FULL MIX tab has no curve yet — analyze-vocal-eq only creates 4 stem curves (vocals/drums/bass/other)",
    "REQUIRES MANUAL SQL: ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS genre_tag text, ADD COLUMN IF NOT EXISTS playlist_name text;",
    "REQUIRES MANUAL SQL: ALTER TABLE projects ADD COLUMN IF NOT EXISTS reference_links jsonb DEFAULT '[]';",
    "OPTIONAL SQL: ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS tonal_balance jsonb, ADD COLUMN IF NOT EXISTS stereo_width float, ADD COLUMN IF NOT EXISTS stereo_width_per_band jsonb, ADD COLUMN IF NOT EXISTS crest_factor_per_band jsonb;",
    "Spotify user token is in-memory only — resets on watcher restart; re-authorize via http://localhost:4242/spotify-auth after restart"
  ],
  "next": "Run manual SQL for missing columns. Consider FULL MIX curve pipeline. FUTURE BUILD: Library Intelligence.",
  "notes": "All Spotify API calls now prefer user OAuth token over client credentials — important for private playlist access."
}
