{
  "updated": "2026-04-19T07:45:00.000Z",
  "last_task": "fix load order: customs from user_settings only",
  "status": "done",
  "working": [
    "DailyTab: loadStaticData() awaited BEFORE load() via async IIFE — race condition fixed",
    "DailyTab: customs/helpers from user_settings are guaranteed set before state spread in load()",
    "DailyTab: seedFixed() runs after loadStaticData completes, so it sees correct customs ids",
    "DailyTab: saveCustoms()/saveHelpers()/saveHelperTicks() are the only write paths",
    "DailyTab: save() upsert no longer includes customs, helpers, or helper_ticks",
    "BrainTab: reference_tracks grouped by collection_name in Brain Entries section",
    "BrainTab: source indicator dots (●/◑/○) and [+] promote button per track",
    "BrainTab: track-dl-btn opens Spotify in new tab AND copies URL",
    "mozartContext: userRefs vs chartRefs split for Mozart context"
  ],
  "broken": [],
  "partial": [
    "SQL migration not run — source/promoted columns don't exist yet in reference_tracks. Run in Supabase SQL Editor: ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS source text DEFAULT 'agent', ADD COLUMN IF NOT EXISTS promoted boolean DEFAULT false; UPDATE reference_tracks SET source = 'agent' WHERE collection_name = 'daily_chart';"
  ],
  "next": "Run SQL migration in Supabase dashboard for source/promoted columns on reference_tracks",
  "notes": "Root cause of customs reset: loadStaticData() and load() were called without await, racing. load() did state={...state,...} spreading customs:[] before user_settings resolved. Fixed by IIFE: await loadStaticData() then load()."
}
