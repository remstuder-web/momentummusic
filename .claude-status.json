{
  "updated": "2026-04-26T06:25:00Z",
  "last_task": "analyzer-five-fixes",
  "status": "done",
  "working": [
    "FIX 1: VocalEqChart — getCurveArray() handles array/object/numeric-key/stems formats; normalizePeak() aligns both curves to 0dB peak before display; debug console.log on refCurve",
    "BUILD 2: save-audio passes song_id from ProjectsTab, watcher uses it as primary lookup (fallback: code prefix) for work_data analysis save",
    "BUILD 3: ref dropdown replaced with searchable picker — text input filters by artist/title/genre/playlist, shows BPM + tags, per-song open state, click-outside closes",
    "BUILD 4: loadVocalEq auto-selects first project/song ref when ANALYZER opens if none selected",
    "BUILD 5: POST /generate-proq4-preset endpoint writes .ffp XML to Desktop; Pro-Q 4 button in ANALYZER shown when mix+ref curves both present",
    "Briefing gear staleness filter: items >48h removed, 24-48h labeled [OUTDATED]",
    "TikTok tracks + brain synthesis in morning briefing",
    "Tier 1 (auto): Spotify preview+genres → Essentia BPM/key/energy/tonal_balance/stereo_width",
    "Tier 2 (on-demand): POST /analyze-stems with Demucs"
  ],
  "broken": [],
  "partial": [
    "Spotify rate limited until ~2026-04-26T20:13 UTC — Tier 1 queue paused",
    "Pro-Q 4 .ffp format: XML format is plausible but untested against actual FabFilter parser — test by importing preset in Pro-Q 4",
    "OPTIONAL SQL: ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS tonal_balance jsonb, stereo_width float, stereo_width_per_band jsonb, crest_factor_per_band jsonb",
    "OPTIONAL SQL: ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS genre_tag text, ADD COLUMN IF NOT EXISTS playlist_name text"
  ],
  "next": "Test Pro-Q 4 preset import in FabFilter. Check ANALYZER: open a song, verify ref picker search works, confirm auto-select fires, verify EQ curves align correctly (peak at 0dB). Wait for Spotify rate limit to clear then queue will auto-process.",
  "notes": "refCurve debug console.log is intentionally left in for now — remove after format confirmed. Pro-Q 4 .ffp format may need adjustment if FabFilter uses binary chunk format instead of XML."
}
