{
  "updated": "2026-04-25T23:10:00Z",
  "last_task": "fix-playlist-import-fields-param",
  "status": "partial",
  "working": [
    "fields param removed from /playlists/{id}/tracks URL",
    "playlist name fetch moved before tracks loop",
    "USER_OAUTH token confirmed loading from disk on startup",
    "/me endpoint works (valid user token)",
    "/me/playlists works (playlist-read-private scope confirmed)",
    "/playlists/{id} metadata works with user OAuth token"
  ],
  "broken": [
    "/playlists/{id}/tracks returns 403 for ALL playlists with BOTH client_credentials and user OAuth tokens",
    "This is a Spotify API quota restriction — NOT a code bug"
  ],
  "partial": [
    "Playlist import blocked by Spotify API-level 403 on /tracks endpoint",
    "Root cause: Spotify app likely needs Extended Access (quota mode) — go to https://developer.spotify.com/dashboard → your app → Settings → Request Extended Access",
    "REQUIRES MANUAL SQL: ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS genre_tag text, ADD COLUMN IF NOT EXISTS playlist_name text;",
    "REQUIRES MANUAL SQL: ALTER TABLE projects ADD COLUMN IF NOT EXISTS reference_links jsonb DEFAULT '[]';"
  ],
  "next": "Check Spotify Developer Dashboard for the app (client ID: 45bb7c8c0553458ca79e0848ea805559) → Settings → verify quota mode. May need to request Extended Access to use /playlists/{id}/tracks. Alternatively, use /me/playlists to list and iterate through user's own playlists (those tracks ARE accessible).",
  "notes": "Confirmed via direct curl: /playlists/{id}/tracks returns 403 regardless of token type or market param. /playlists/{id} metadata returns 200. /me/playlists returns 200 with 22 playlists. The tracks restriction is at the Spotify app permissions level."
}
