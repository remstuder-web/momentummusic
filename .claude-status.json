{
  "updated": "2026-04-25T14:37:00Z",
  "last_task": "error-handling-supabase-writes-refs-display",
  "status": "done",
  "working": [
    "21 Supabase writes now have { error } destructuring + console.error logging",
    "Critical writes also log success (reference_tracks credits, speicherbox, Essentia, EQ curves)",
    "REFERENCE LINKS display merges work_data.reference_links (Mozart) + reference_links (manual)",
    "normSongRef() handles Mozart format: {title,artist,spotify_id} → {url, name}",
    "removeSongRef() removes from both columns atomically",
    "Refresh after Mozart action fetches both work_data and reference_links columns",
    "svelte-check: 0 errors, pm2 OK"
  ],
  "broken": [],
  "partial": [
    "BUILD 2/3: feedback_patterns and chart_history tables still need SQL in Supabase",
    "genres column: ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS genres jsonb;"
  ],
  "next": "Run ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS genres jsonb; in Supabase, then POST /enrich-library-genres to backfill",
  "notes": "Mozart refs (work_data.reference_links) now visible in the REFERENCE LINKS block. The two sources are distinct: work_data has title/artist/spotify_id, top-level has url/name. normSongRef bridges them for display."
}
