{
  "updated": "2026-04-19T11:10:00.000Z",
  "last_task": "brain: rotating placeholder, confidence pills, hover preview",
  "status": "done",
  "working": [
    "BrainTab: DUMP_HINTS rotates every 4s via onMount setInterval, bound to textarea placeholder",
    "BrainTab: cycleConfidence() replaces handleConfidenceClick — single pill cycles levels, tooltip only for locked",
    "BrainTab: conf-pill CSS — weak=grey, medium=dim, strong=gold, locked=green",
    "BrainTab: brain-entry-title gets title attr for native 120-char hover preview",
    "DailyTab: today focus pin task with gold focus-block",
    "DailyTab: delete agent outputs + permanent download dismiss"
  ],
  "broken": [],
  "partial": [
    "SQL migration not run — source/promoted columns don't exist yet in reference_tracks. Run in Supabase SQL Editor: ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS source text DEFAULT 'agent', ADD COLUMN IF NOT EXISTS promoted boolean DEFAULT false; UPDATE reference_tracks SET source = 'agent' WHERE collection_name = 'daily_chart';"
  ],
  "next": "Run SQL migration in Supabase dashboard for source/promoted columns on reference_tracks",
  "notes": "CONF_DISPLAY maps low→weak for display only; stored value in DB is still 'low'"
}
