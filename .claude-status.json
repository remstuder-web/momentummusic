{
  "updated": "2026-04-19T11:31:00.000Z",
  "last_task": "five fixes: purple, demo count, kworb, agent font, newlines",
  "status": "done",
  "working": [
    "DailyTab: briefing block background transparent, border #252525 (no purple)",
    "DailyTab: briefing label color gold rgba(201,168,76,.6)",
    "DailyTab: .agent-output * inherits DM Sans weight 300; agent-header Space Mono preserved",
    "parseAgentOutput: splits embedded '- ' bullet runs within a single line",
    "mozartContext: 4 formatting rules enforcing one-point-per-line + section spacing",
    "morning-briefing: demoCount/projectSongCount via PostgREST count=exact header",
    "fetchKworbSpotify: stateful regex returns {position, artist, title, streams}",
    "kworbSPText formatted as 'N. Artist — Title' in scout prompt"
  ],
  "broken": [],
  "partial": [
    "SQL migration not run — source/promoted columns don't exist yet in reference_tracks. Run in Supabase SQL Editor: ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS source text DEFAULT 'agent', ADD COLUMN IF NOT EXISTS promoted boolean DEFAULT false; UPDATE reference_tracks SET source = 'agent' WHERE collection_name = 'daily_chart';"
  ],
  "next": "Run SQL migration in Supabase dashboard for source/promoted columns on reference_tracks",
  "notes": "kworb scrapers depend on kworb HTML structure — if they return empty, the scout prompt gracefully shows 'Unavailable'. Test by running /agent-scout manually."
}
