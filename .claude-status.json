{
  "updated": "2026-04-23T12:33:30.000Z",
  "last_task": "personal-checkbox-persist",
  "status": "partial",
  "working": [
    "personal checkbox onchange already calls updateField(conn, 'personal', e.target.checked)",
    "updateField does supabase.from('connections').update({ [field]: value }).eq('id', conn.id)",
    "Watcher startup check logs SQL warning if personal column missing"
  ],
  "broken": [],
  "partial": [
    "personal column missing from connections table in Supabase — saves will fail silently until added"
  ],
  "next": "Run in Supabase SQL editor: ALTER TABLE connections ADD COLUMN IF NOT EXISTS personal boolean DEFAULT false;",
  "notes": "Cannot run DDL via REST API — requires Supabase dashboard. Watcher now logs the exact SQL on startup."
}
