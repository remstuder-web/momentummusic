{
  "updated": "2026-04-24T22:00:00.000Z",
  "last_task": "mozart-no-analysis-eq-chart-persist-category-chips",
  "status": "done",
  "working": [
    "Mozart/ANALYZER: never outputs raw BPM/LUFS/danceability/energy numbers unless explicitly asked",
    "VocalEqChart: keep-last-value pattern — chart persists on collapse/reopen",
    "VocalEqChart: mix line bright white rgba(255,255,255,0.85), ref line gold #c9a84c, zero line visible rgba(255,255,255,0.3)",
    "BrainTab: category chips in approval panel — suggested cat first, 8 chips, clickable to reassign",
    "Watcher /suggest-category: returns allCategories array for chip pool",
    "ANALYZER ref dropdown — shows library tracks processed by background pipeline",
    "Credits panel below chart (producer/mixer/mastering) when ref selected + credits exist",
    "processLibraryTrackInBackground(): preview URL → Essentia → Demucs EQ curves → Genius credits",
    "Background queue runs on startup (15s delay), 5s gap between tracks",
    "GET /processing-queue: {queued, processing, next}",
    "POST /fetch-credits: {title, artist, reference_track_id} → Genius scrape + saves to reference_tracks",
    "saveToCheckout(): dedup wrapper for all reference_tracks inserts",
    "BrainTab library sort by date/artist",
    "getCrossChartTopics() wired into morning briefing CHART PULSE, scout prompt, Mozart context",
    "cleanup-brain-dupes uses supabaseAdmin (bypasses RLS)",
    "Brain dupe root cause fixed: date-prefix strip in saveBrainFile + bare-title fallback in syncObsidianFile",
    "8/8 tests passing"
  ],
  "broken": [],
  "partial": [],
  "next": "Run SQL in Supabase dashboard to add missing columns:\nALTER TABLE vocal_eq_curves ADD COLUMN IF NOT EXISTS reference_track_id text;\nALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS credits jsonb, ADD COLUMN IF NOT EXISTS emotional_arc jsonb;\nThen promote some library tracks to My Refs and monitor background queue: curl localhost:4242/processing-queue",
  "notes": "Background queue only processes tracks with spotify_id and source in (user, agent). Queue will be empty until SQL columns exist AND library tracks have been promoted to My Refs. Genius scraping may fail for non-English tracks or tracks not on Genius."
}
