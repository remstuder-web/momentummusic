{
  "updated": "2026-04-27T12:53:00Z",
  "last_task": "bg-queue-stop",
  "status": "done",
  "working": [
    "bg queue immediately paused + cleared on startup (bgQueuePaused=true + processingQueue.length=0 via /pause-bg-queue)",
    "runBackgroundQueue: max 20 tracks/day, 30s between tracks, 1h retry on rate limit",
    "startup Tier1 + genres enrichment both skip if bgQueuePaused or rate limited",
    "GET /pause-bg-queue: pauses + clears queue",
    "GET /resume-bg-queue: resumes, resets daily count",
    "Telegram /pausequeue and /resumequeue commands working",
    "Queue currently PAUSED — Spotify rate limited"
  ],
  "broken": [],
  "partial": [],
  "next": "When Spotify rate limit clears tomorrow, use /resumequeue on Telegram or GET /resume-bg-queue to restart processing (max 20 tracks that day)",
  "notes": "bgQueuePaused starts false on each pm2 restart — call /pause-bg-queue immediately after restart while rate limited (already done this session). Queue cleared: 0 tracks pending."
}
