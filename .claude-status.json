{
  "updated": "2026-04-26T06:38:00Z",
  "last_task": "song-detail-ui-4-changes",
  "status": "done",
  "working": [
    "SONG NOTES collapsed by default; auto-opens if wd.project_info has content",
    "REFERENCE LINKS label removed from song detail",
    "URL input hidden behind '+ add ref' toggle button with cancel",
    "YOUR MIX / INSTRUMENTAL drop zone labels: 11px, color #9e9690",
    "FIX 1: VocalEqChart normalizePeak() aligns both curves to 0dB peak",
    "BUILD 3: ref searchable picker in ANALYZER",
    "BUILD 4: auto-select first project/song ref on ANALYZER expand",
    "BUILD 5: POST /generate-proq4-preset endpoint + button in ANALYZER",
    "Briefing gear staleness filter + TikTok data in morning briefing",
    "Tier 1 auto-analysis queue (449 tracks, paused for Spotify rate limit)"
  ],
  "broken": [],
  "partial": [
    "Spotify rate limit until ~2026-04-26T20:13 UTC — Tier 1 queue paused, auto-resumes",
    "Pro-Q 4 .ffp format: XML format untested against actual FabFilter parser",
    "OPTIONAL SQL: ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS tonal_balance jsonb, stereo_width float, stereo_width_per_band jsonb, crest_factor_per_band jsonb",
    "OPTIONAL SQL: ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS genre_tag text, ADD COLUMN IF NOT EXISTS playlist_name text"
  ],
  "next": "Test UI in browser: notes toggle, + add ref button, drop zone labels. Test Pro-Q 4 preset import in FabFilter. Monitor Spotify queue after rate limit clears (~14h from 06:13 UTC).",
  "notes": "Song notes open state derived from wd.project_info existence — no extra DB field needed. addingRef state is per-song. Both new state dicts use spread-to-trigger reactivity pattern."
}
