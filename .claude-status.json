{
  "updated": "2026-04-25T16:00:00Z",
  "last_task": "spotify-playlist-import",
  "status": "done",
  "working": [
    "POST /import-spotify-playlist: paginates playlist tracks, dedupes by spotify_id, fetches artist genres, inserts to reference_tracks",
    "importSpotifyPlaylist(): saves genre_tag, playlist_name, genres[], popularity, approved=true",
    "Startup SQL check warns if genre_tag/playlist_name columns missing",
    "BrainTab library: Genre sort button added (date/artist/genre)",
    "BrainTab library: libraryGenreFilter state + genre-filter-chips row",
    "BrainTab library: filteredLibraryRefs filters by genre (genres[] OR genre_tag)",
    "BrainTab library: track-genre-tag chip on each row",
    "svelte-check: 0 errors, pm2: ok"
  ],
  "broken": [],
  "partial": [
    "REQUIRES MANUAL SQL: ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS genre_tag text, ADD COLUMN IF NOT EXISTS playlist_name text;",
    "REQUIRES MANUAL SQL: ALTER TABLE projects ADD COLUMN IF NOT EXISTS reference_links jsonb DEFAULT '[]';",
    "BUILD 2/3: feedback_patterns and chart_history tables still need SQL in Supabase"
  ],
  "next": "Run the two ALTER TABLE statements in Supabase, then test: POST /import-spotify-playlist with a playlist URL to verify tracks save with genre_tag and appear in BrainTab Library with genre filter chips.",
  "notes": "Playlist import rate-limits: 200ms after artist fetch, 300ms after each track insert. Skips duplicates by spotify_id."
}
