{
  "updated": "2026-04-25T14:28:00Z",
  "last_task": "mozart-refs-refresh-dedup",
  "status": "done",
  "working": [
    "sendAI() refreshes song.work_data after add_project_reference success (updates songs state + expandedSong.work_data)",
    "normalizeTrack dedup on every add_project_reference save (prefer entry with spotify_id when deduping existing refs)",
    "POST /fix-song-refs: removes Test Artist entries, deduplicates by normalized key, prefers spotify_id entries",
    "Song 152 cleaned: 4→3 refs (Test Artist removed)",
    "svelte-check: 0 errors, pm2 OK"
  ],
  "broken": [],
  "partial": [
    "BUILD 2/3: feedback_patterns and chart_history tables still need SQL in Supabase",
    "genres column: ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS genres jsonb;"
  ],
  "next": "Run ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS genres jsonb; in Supabase, then POST /enrich-library-genres to backfill",
  "notes": "Song 152 still has two 'Morgen' entries: Apache (no spotify_id) and Apache 207 (has spotify_id) — kept both since they normalize differently. If user wants to merge, run fix-song-refs again after confirming they're the same track."
}
