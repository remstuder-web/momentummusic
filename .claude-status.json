{
  "updated": "2026-04-23T21:20:00.000Z",
  "last_task": "floating-link-popup",
  "status": "done",
  "working": [
    "floating-link-popup: global a[target=_blank] interceptor opens iframe popup (85vw/85vh), cross-origin fallback message, ↗ opens real new tab",
    "scout-extract-mentions: extractTracksFromText() saves all text-mentioned tracks to checkout via Haiku",
    "brain-loadentries-onmount: onMount async, awaits loadEntries()",
    "fix-reference-tracks-order: reference_tracks ordered by created_at DESC",
    "scout-tracks-checkout: /agent-scout saves kworb tracks to checkout",
    "sync-project-refs: POST /sync-project-refs → project_meta.reference_links → reference_tracks",
    "fix-vocal-eq-header-click: z-index fix on vocal-eq-section/header",
    "vocalEqStatus per-song + reset button",
    "remove-checkout-banner: CHECK OUT TODAY banner + pulse dot removed"
  ],
  "broken": [],
  "partial": [
    "vocal-eq-stems: SQL migration not yet run — vocal_eq_curves missing stem_type and source_type columns",
    "checkout_date column: may not exist in reference_tracks — run SQL: ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS checkout_date timestamptz;"
  ],
  "next": "Run both SQL migrations in Supabase dashboard: (1) ALTER TABLE vocal_eq_curves ADD COLUMN IF NOT EXISTS stem_type text DEFAULT 'vocals', ADD COLUMN IF NOT EXISTS source_type text DEFAULT 'mix'; (2) ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS checkout_date timestamptz;",
  "notes": "Link popup: cross-origin detection via contentDocument access throws — caught in onload, sets popupBlocked=true to show fallback. Cleanup removes both event listeners on destroy."
}
