{
  "updated": "2026-04-25T14:19:00Z",
  "last_task": "mozart-actions-refs-untitled-projects",
  "status": "done",
  "working": [
    "Song REFERENCE LINKS block: always visible regardless of stage or existing links",
    "Add form always shown (removed production-stage gate)",
    "/mozart-action add_project_reference: resolves project_name → song_id via ilike title search, falls back to most recent null-title song if no title match",
    "svelte-check: 0 errors",
    "pm2 restart OK"
  ],
  "broken": [],
  "partial": [
    "BUILD 2/3: feedback_patterns and chart_history tables still need SQL in Supabase",
    "genres column: ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS genres jsonb;"
  ],
  "next": "Run ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS genres jsonb; in Supabase, then POST /enrich-library-genres to backfill",
  "notes": "project_name fallback: tries ilike match first, then null-title fallback. Does NOT filter by project_id — assumes Mozart is working in the context of the active project."
}
