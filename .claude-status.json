{
  "updated": "2026-05-03T15:38:00Z",
  "last_task": "references-overhaul",
  "status": "done",
  "working": [
    "addSongRef: now uses /spotify-track-meta (not oEmbed), adds id field, fires analyze-spotify-track for library sync",
    "addRefLink: now updates local state immediately (refs appear without refresh), clears input correctly (refLinkInput[p.id]), fires analyze-spotify-track for library sync",
    "/analyze-spotify-track: upserts to reference_tracks after full analysis (source=user, collection_name=project_ref)",
    "/analyze-spotify-track: lightweight path — if spotify_id provided without url, upserts metadata stub and returns immediately",
    "/spotify-track-meta: ensureSpotifyToken() called before Spotify fetch — handles expired tokens via refresh or client credentials fallback",
    "spotifyTokenExpiresAt tracked in loadSpotifyToken — expiry-aware token management",
    "pm2 restarted, ping OK, /spotify-track-meta returning title+artist correctly"
  ],
  "broken": [],
  "partial": [],
  "next": "Test in UI: paste Spotify URL in project References tab and song ref field — both should resolve name immediately and sync to library",
  "notes": "Previous test 404 was a bad track ID, not a token bug. Token refresh added as defensive measure anyway."
}
