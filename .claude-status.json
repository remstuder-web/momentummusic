{
  "updated": "2026-04-25T14:42:00Z",
  "last_task": "analyzer-mozart-insight-panel",
  "status": "done",
  "working": [
    "loadMozartInsight(songId, refTrack): calls Claude Haiku, returns {strategic[], creative[], next_step}",
    "Auto-fires when ref dropdown changes alongside loadVocalEq()",
    "Mozart Insight panel renders below EQ chart + credits, above SUCCESS MATCH",
    "STRATEGIC CONTEXT / CREATIVE DIRECTION / NEXT STEP sections with gold section labels",
    "Cost tracked via /track-cost at Haiku rates",
    "State: mozartInsight, mozartInsightLoading per song.id",
    "svelte-check: 0 errors"
  ],
  "broken": [],
  "partial": [
    "BUILD 2/3: feedback_patterns and chart_history tables still need SQL in Supabase",
    "genres column: ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS genres jsonb;"
  ],
  "next": "Run ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS genres jsonb; in Supabase, then POST /enrich-library-genres to backfill",
  "notes": "Mozart insight requires API key in localStorage (mm_api_key). If key missing, function returns early silently. Panel only shows after ref is selected from dropdown."
}
