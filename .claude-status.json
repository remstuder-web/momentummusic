{
  "updated": "2026-04-25T23:30:00Z",
  "last_task": "import-all-user-playlists-v2",
  "status": "done",
  "working": [
    "importAllUserPlaylists(): 434 tracks saved from 21 user-owned playlists",
    "playlist.items.href used (not tracks.href — that field is null in API response)",
    "/playlists/{id}/items endpoint works for user-owned playlists (not /tracks)",
    "i.item field used (not i.track — /items endpoint uses different field name)",
    "genre_tag = playlist name lowercase (ELECTRONIC EDM → 'electronic edm')",
    "genres = Spotify artist genres per track (cached per artist per session)",
    "BrainTab: playlist_name shown gold (.playlist-tag), genres[0] shown dim (.track-genre-tag)",
    "2 followed playlists (not owned) correctly skip with 403 — expected behavior"
  ],
  "broken": [],
  "partial": [
    "artist genres fetch (per-track) may be rate-limited on large playlists — caching mitigates this",
    "REQUIRES MANUAL SQL: ALTER TABLE reference_tracks ADD COLUMN IF NOT EXISTS genre_tag text, ADD COLUMN IF NOT EXISTS playlist_name text; (needed before import works fully)",
    "Followed playlists (not user-owned) still 403 — needs Spotify Extended Access to import those"
  ],
  "next": "Run manual SQL for missing columns if not done yet. Re-run /import-all-my-playlists after SQL. Brain tab library should now show playlist labels in gold and Spotify genres in dim.",
  "notes": "Key discoveries: (1) API returns items.href not tracks.href in me/playlists response. (2) /items endpoint uses i.item not i.track. (3) Only user-owned playlists work — followed playlists still 403. 434 tracks imported with genre labels preserved."
}
